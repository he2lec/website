import React from 'react';
import styled from '@emotion/styled';
import tw from 'tailwind.macro';

const Container = styled.div`
  ${tw`text-center my-12`}
`;

const Skills = () => (
  <Container>
    <strong>Skills:</strong> Coming soon.
  </Container>
);

export default Skills;
